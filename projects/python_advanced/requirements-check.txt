# Pinned dependencies for check_snippets.py's third-party runner.
#
# These are NOT dependencies of the site or of any demo app. They exist so a
# block marked `<!-- needs: numpy -->` can actually be EXECUTED rather than
# merely parsed — half of /python-advanced is about libraries, and a library
# example that is never run is exactly where a stale API call hides.
#
# Pinned, because an unpinned upgrade would silently change what the posts are
# verified against. Bump deliberately, then re-run check_snippets.py.
numpy==2.1.3
pandas==2.2.3
scikit-learn==1.5.2
PyYAML==6.0.2
marshmallow==3.23.1
SQLAlchemy==2.0.36
mysql-connector-python==9.1.0
