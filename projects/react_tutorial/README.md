# React Tutorial

## Where ?
- https://lovemesomecoding.com/react

## Requirements
- Use https://react.dev and https://www.w3schools.com/react/default.asp and put together a couple of the main topics. We dont need to include everything.
- Use the pizza-react-frontend in the /Users/folaukaveinga/Github/claude_lovemesomecoding/lovemesomecoding_demo_project/pizza project to show example. if an example is not there, find a page that the example can be used for and add it, then create the example snippet from there. After adding or updating the pizza-react-frontend project make sure to run it and make sure you did not break anything.
- Create the list of main topics to cover so that the next time we update to the latest version it will just be an update and add.