# The pre-publish snapshot of the seven prod posts and the four derived indexes,
# taken 2026-08-22 before `seed.py --env prod --write`. It is the rollback path
# (the content-DB bucket has NO versioning), so it stays on disk — but it is a
# migration artifact and does not belong in the repo.
.backup-prod-*/
__pycache__/
